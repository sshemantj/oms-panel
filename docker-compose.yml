services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ss-container-oms-panel
    ports:
      - "4000:3000"
    restart: always
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
